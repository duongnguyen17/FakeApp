import React from 'react';
import {StyleSheet, SafeAreaView} from 'react-native';

const PostDetail = () => {
  return <SafeAreaView style={StyleSheet.container}></SafeAreaView>;
};

export default PostDetail;

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
